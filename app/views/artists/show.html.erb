<h1><%= @artist.name %></h1>
<ul>
  <% @artist.songs.each do |song| %>
    <li><%= link_to song.title, artist_song_path(@artist, song) %></li>
    #to have_link(@song.title, href: artist_song_path(@artist, @song))

  <% end %>
</ul>
